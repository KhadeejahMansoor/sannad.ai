'use client';
import React from 'react';

/* ------------------------------------------------------------------ *
 * ArticlesView
 * ------------------------------------------------------------------
 * Empty for now. This previously rendered sixteen cards of lorem ipsum
 * — one real title with placeholder body text, plus fifteen generated
 * duplicates — all of it visible on the live site.
 *
 * To bring articles back, restore an `articles` array and map over it;
 * ArticleVideo.js is still in the codebase for the embeds.
 * ------------------------------------------------------------------ */

export default function ArticlesView() {
  return <p className="text-[15px] text-[#A8A29E]">Nothing here yet.</p>;
}